import { Router } from 'express';

const router = Router();

// TODO(phase-4): add admin endpoints for email provider config and template previews.
// TODO(phase-4): add secured endpoint to trigger test email delivery.

export default router;
